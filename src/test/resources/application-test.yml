spring:
  datasource:
    url: jdbc:tc:postgresql:17:///db_ikon_brasil_test?TC_TMPFS=/testtmpfs:rw&currentSchema=public
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
